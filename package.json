{
    "name": "pulumi-azure-demo",
    "scripts": {
        "pulumi:up": "pulumi up -f -y --suppress-outputs",
        "deploy": "npm run pulumi:up && ./deploy.sh"
    },
    "devDependencies": {
        "@types/node": "^14"
    },
    "dependencies": {
        "@pulumi/pulumi": "^3.0.0",
        "@pulumi/azure-native": "^1.0.0"
    }
}
